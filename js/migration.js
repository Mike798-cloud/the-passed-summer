// Legacy module retained only for overlay compatibility with older deployments.
// The current storyline no longer uses migration decisions as an ending.
export function choose(){return false}
export function endingHTML(){return ''}
