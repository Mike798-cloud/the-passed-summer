import {pageRoot,crumb,refreshShared,safeText,maybeAutoSupportPrompt} from './app.js';
import {loadIndex,runSearch,suggestions,remember,resultHTML,recentSearches,epilogueHTML} from './search.js';
import {visit,plotStage,reviewReady,finalChoice,recentPages} from './state.js';
(async()=>{await loadIndex();visit('search_page');const params=new URLSearchParams(location.search);let q=params.get('q')||'';
 const render=()=>{document.title=q?`${q} - 站内搜索｜栖岚实验高级中学`:'站内搜索｜栖岚实验高级中学';const results=q?runSearch(q):[],recent=recentSearches(),pages=recentPages().slice(0,6),stage=plotStage();pageRoot().innerHTML=`${crumb([{label:'站内搜索'}])}<section class="panel"><h2 class="section-title">站内搜索 <small>通知 / 服务 / 公开归档</small></h2><form id="search-form"><div class="searchbar"><input id="q" class="form-control" value="${safeText(q)}" autocomplete="off" placeholder="输入服务名称、年份、班级或公开资料关键词"><button class="btn primary" type="submit">搜索</button></div><div id="suggestions" class="search-suggestions" hidden></div></form>${q?`<div class="divider"></div><div class="small muted">“${safeText(q)}” · 共 ${results.length} 条站内结果</div>${resultHTML(results)}${reviewReady()&&!finalChoice()?'<div class="notice-box"><strong>学生事务：</strong>当前会话存在一项待确认的补充核验事项。<a href="review.html">查看事项</a></div>':''}${epilogueHTML()}`:`<div class="portal-meta"><div><span>索引范围</span><b>通知与服务</b></div><div><span>公开归档</span><b>${stage>=3?'2017–2026':'2019–2026'}</b></div><div><span>更新状态</span><b>已同步</b></div></div><h3 class="subhead">常用检索</h3><div class="actions"><a class="btn" href="search.html?q=2026%20秋季返校安排">2026 秋季返校安排</a><a class="btn" href="search.html?q=住宿服务">住宿服务</a><a class="btn" href="search.html?q=校车班次">校车班次</a><a class="btn" href="search.html?q=校园广场">校园广场</a></div><div class="search-history-grid"><div><h3 class="subhead">最近搜索</h3><div class="recent-stack">${recent.map(x=>`<a href="search.html?q=${encodeURIComponent(x)}"><b>${safeText(x)}</b><span>再次检索</span></a>`).join('')||'<span class="small muted">暂无最近搜索</span>'}</div></div><div><h3 class="subhead">最近访问</h3><div class="recent-stack">${pages.map(x=>`<a href="${safeText(x.url)}"><b>${safeText(String(x.title).replace(/｜栖岚实验高级中学.*$/,''))}</b><span>返回刚才查看的页面</span></a>`).join('')||'<span class="small muted">暂无最近访问</span>'}</div></div></div>`}</section>`;bind();refreshShared();maybeAutoSupportPrompt()};
 const bind=()=>{const form=document.getElementById('search-form'),input=document.getElementById('q'),box=document.getElementById('suggestions');const upd=()=>{const arr=suggestions(input.value);box.innerHTML=arr.map(x=>`<button type="button" class="${/不要去|失踪|他们会送|▒/.test(x)?'suggestion-glitch':''}">${safeText(x)}</button>`).join('');box.hidden=!arr.length;box.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>{input.value=b.textContent;form.requestSubmit()}))};input.addEventListener('input',upd);input.addEventListener('focus',upd);input.addEventListener('blur',()=>setTimeout(()=>box.hidden=true,120));form.addEventListener('submit',e=>{e.preventDefault();q=input.value.trim();if(!q)return;remember(q);location.href=`search.html?q=${encodeURIComponent(q)}`})};
 if(q)remember(q);render();
})();
